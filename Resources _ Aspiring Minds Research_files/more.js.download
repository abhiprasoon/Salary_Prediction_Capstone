jQuery(document).ready(function(){
    jQuery("a").click(function(event){
	    var cID = event.target.id;
		//alert(cID);
		
		if(cID.split('_')[1]=='close'){
			    jQuery('#'+(cID.split('_')[0])+'_data').slideToggle();
                //alert(cID.split('_')[0]);				
                jQuery('#'+(cID.split('_')[0])).show();				 
		}else{
		    jQuery('#'+cID).hide();
		    jQuery('#'+cID+'_data').slideToggle();
		
		}
		
		
	    }); 

    jQuery(".shadow").click(function(event){
	    var id = $(this).attr('id');
		//alert(id);
		jQuery('#'+id).toggleClass("protate");
	    });		
		
	 $(".shadow").hover(function(event){
	       var id = $(this).attr('id');
		   //alert(id);
		  $('#'+id).css("cursor","pointer");
		  },function(){
		  $('#'+id).css("cursor","auto");
		}); 
	
	    /****
	    Bar chart on the first blog - 'A re-beginning : Welcome to AM Research!'
	
	    *****/
		var graphdef = {
			categories : ['Tier of colleges', 'College GPA  (1 pt)'],
			dataset : {
				'Tier of colleges' : [
					{ name : 'Salary in jobs', value : 26 },
					{ name : 'Odds to get a job', value : 24 }
				],
				
				'College GPA  (1 pt)' : [
					{ name : 'Salary in jobs', value : 9 },
					{ name : 'Odds to get a job', value : 16 }
				]
				
			}
		}
	   
	   var chart = uv.chart('StackedBar', graphdef, {
												meta : {
													caption : '',
													subcaption : '',
													hlabel : 'Percent(%)',
													vlabel : '',
													vsublabel : ''
												}
								});
	
    });